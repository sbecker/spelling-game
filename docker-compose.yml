services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: spelling
      POSTGRES_PASSWORD: spelling
      POSTGRES_DB: spelling_game
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
